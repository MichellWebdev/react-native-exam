import React, { useState } from 'react';
import { StyleSheet, View } from 'react-native';

interface PostBoxLabels {
}

const PostBox = ({
}: PostBoxLabels) => {

    return (
        <View>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default PostBox;
